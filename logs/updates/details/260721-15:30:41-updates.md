# 260721-15:30:41 — Restore credential parser fixture

## Motivation
The full backend suite referenced a non-secret `.env-raw` parser fixture that was absent from the repository, leaving three otherwise unrelated credential tests failing.

## Changes
- `tests/fixtures/env_raw_sample.txt`: added placeholder-only token and endpoint values expected by the parser tests.

## Design decisions
The fixture contains no real credentials and remains force-tracked despite the repository rule that normally ignores credential-shaped fixture files.

## Verification
The credential parser tests and full backend suite are run before merge.

## Follow-ups
none
