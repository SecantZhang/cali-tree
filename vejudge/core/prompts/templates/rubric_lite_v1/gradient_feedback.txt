The global Semantic Consistency rubric predicted "{prediction}" but the human target is
"{target}" for this training example.

Instruction: {instruction}
Judge rationale: {rationale}

Diagnose the reusable boundary error: missing evidence (no), incomplete recognizable evidence
(partial), fully followed overall idea (yes), or source-scene replacement. Improve the global
rubric so this class of visual error is handled consistently. Do not mention item IDs, editor
names, dataset frequencies, or infer labels from instruction wording. Preserve the ordinal
condition-evidence JSON contract.
