Improve the reusable one-call evidence-ledger rubric from this calibration error.

Instruction: {instruction}
Prediction: {prediction}
Human target: {target}
Judge rationale: {rationale}

Diagnose whether the error came from atomic condition decomposition, requested-scope
coverage, final semantic identity, residual old content, action/relation evidence, scene
validity, or confusing generation quality with a semantic defect. Return a complete
general rubric, never item identifiers, editor names, dataset priors, or memorized image
details. Preserve the `rubric-lite-evidence-ledger-v6` JSON contract, its at-most-four
condition ledger, residual_type enum, semantic_completion range, scene_validity, label, and
rationale.
