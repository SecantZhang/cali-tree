Merge the two calibrated ImagenHub Semantic Consistency rubrics into one reusable rubric.
Preserve condition decomposition, the no/partial/yes tie breaks, editing-scene preservation,
and the compact JSON contract. Do not introduce standalone perceptual-quality scoring or
case/editor-specific rules. If their semantic requirements are incompatible, report the
conflict rather than forcing a merge.

Return JSON only:
{{"conflict":false,"conflict_reason":"","prompt":"merged rubric"}}

RUBRIC A:
{left}

RUBRIC B:
{right}
