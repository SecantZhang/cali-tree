Reconcile these two decomposed ImagenHub Semantic Consistency rubrics into one reusable
rubric. Preserve rubric_version "sc-v3", all rubric_scores fields and allowed values, and the
deterministic no/partial/yes mapping. Generalize compatible boundary refinements. If they
assign incompatible meanings or label mappings to the same field state, report the conflict
instead of averaging it away. Never add editor/item rules or standalone perceptual-quality
scoring.

Return JSON only:
{{"conflict":false,"conflict_reason":"","prompt":"merged decomposed rubric"}}

RUBRIC A:
{left}

RUBRIC B:
{right}
