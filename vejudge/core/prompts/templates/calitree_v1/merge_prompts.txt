Merge the two calibrated image-judge rubrics into one general rubric. Preserve compatible
criteria, decision priorities, and the exact JSON label/rationale contract. If their
requirements are incompatible, report the conflict rather than forcing a merge.

Return JSON only:
{{"conflict":false,"conflict_reason":"","prompt":"merged rubric"}}

RUBRIC A:
{left}

RUBRIC B:
{right}
