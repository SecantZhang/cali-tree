Merge these fulfillment-first ImagenHub SC rubrics. Preserve the direct none→no,
partial→partial, full→yes mapping and fulfillment/label/rationale JSON fields. Generalize
compatible visible-evidence boundaries. Report a conflict if the same evidence state maps to
different labels. Never add item/editor rules or standalone perceptual-quality scoring.

Return JSON only:
{{"conflict":false,"conflict_reason":"","prompt":"merged rubric"}}

RUBRIC A:
{left}

RUBRIC B:
{right}
