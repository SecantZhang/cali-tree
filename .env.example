# Copy to .env in the project root. Keys are loaded only by the backend.
OPENAI_API_KEY=
GEMINI_API_KEY=

# Optional API base URL overrides. Leave unset to use the official endpoints.
# OPENAI_BASE_URL=https://api.openai.com/v1
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
